<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title><%= @post['title'] %> - きらきらミニブログ ✨</title>
    <link rel="stylesheet" href="/css/style.css"> 
    <link rel="stylesheet" href="/css/show.css"> 
</head>
<body>

    <h1 style="color: #7b4397; text-align: center;"><%= @post['title'] %></h1>

    <div class="content-box">
        <%= @post['content'] %>
    </div>

    <hr style="border-top: 1px solid #ccc; margin: 30px 0;">

    <p class="action-links">
        <a href="/edit/<%= @post['id'] %>">✏️ 記事を編集</a> 
        
        <form action="/delete/<%= @post['id'] %>" method="POST" style="display:inline;">
            <button type="submit" 
                    style="padding: 8px 12px; background-color: #ff99c8; color: white; border: none; border-radius: 15px; font-weight: bold; cursor: pointer;"
                    onclick="return confirm('この記事を削除しますか？')"
            >🗑️ 削除</button>
        </form>
        
        <a href="/">🏠 一覧に戻る</a>
    </p>

</body>
</html>